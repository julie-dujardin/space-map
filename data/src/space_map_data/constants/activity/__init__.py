"""Curated facts about what bodies are still doing.

Three tables, one question: is there heat left inside, and does it get out.
`tidal` is the supply on small worlds, `volcanism` is what it builds at the
surface (including tectonics), `magnetism` is the same heat leaving a core by
convection, or the remanence left where it no longer does.

`schema` defines all three, including the `Measurement` wrapper that keeps a
bound, a model and an observation from reading alike.
"""
