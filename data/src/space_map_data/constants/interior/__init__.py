"""Curated facts about what bodies are made of below the atmosphere.

Two ways in, because the evidence comes two ways. `bodies` holds per-body
layer models for the few dozen objects a spacecraft or a seismometer has
actually constrained; `taxonomy` turns a spectral class into its meteorite
analogue's bulk chemistry for the ~170k asteroids that only have a spectrum.
The second is an estimate and ships labelled as one.

`schema` defines both, including the coarse material vocabulary that lets a
metal core and an ice shell share one bar.
"""
