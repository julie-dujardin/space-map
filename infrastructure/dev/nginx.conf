server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        autoindex on;
        autoindex_format json;
        # The SDK demo runs on its own origin.
        add_header Access-Control-Allow-Origin * always;
    }
}
