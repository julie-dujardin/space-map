name = "space-map"
main = ".svelte-kit/cloudflare/_worker.js"
# Env lives in the CF dashboard; without this, deploys would wipe it.
keep_vars = true
compatibility_date = "2026-04-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".svelte-kit/cloudflare"
binding = "ASSETS"
